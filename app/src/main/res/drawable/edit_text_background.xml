<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Kenarlık rengi -->
    <stroke
        android:width="1dp"
        android:color="@color/button_text_color_3" />

    <!-- Arka plan rengi -->
    <solid android:color="@android:color/white" />

    <!-- Köşe yuvarlaklığı -->
    <corners android:radius="12dp" />
</shape>
